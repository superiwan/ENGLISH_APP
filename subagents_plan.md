# Subagents 方案（基于需求文档）

## 总协调代理（Orchestrator）
- 目标：统筹任务拆分、接口约定、合并节奏和验收标准。
- 输入：`codex_prompt.md.md` 需求。
- 输出：迭代计划、每日状态、发布检查清单。

## 子代理 1：架构与脚手架代理（App Architect）
- 负责：项目目录与依赖管理。
- 产出：`pubspec.yaml`、`main.dart`、导航与页面挂载结构。
- 验收：项目结构包含 `models / database / pages / utils`。

## 子代理 2：数据层代理（Data & SQLite）
- 负责：SQLite 建表、DAO、初始化与示例数据。
- 产出：`lib/database/app_database.dart`、`lib/models/*.dart`。
- 验收：支持单词 CRUD、错题 upsert、进度统计查询。

## 子代理 3：训练引擎代理（Training Logic）
- 负责：出题策略、熟练度计算、错题更新、拼写判定。
- 产出：选择题与拼写核心逻辑、编辑距离工具。
- 验收：
  - 正确 +10，错误 -15
  - 错题记录 `mistakes` 正确累加
  - 优先错题出题

## 子代理 4：内容导入代理（PDF Import）
- 负责：本地 PDF 选择、文本解析、正则抽取、防重入库、导入进度。
- 产出：`lib/utils/pdf_importer.dart` + 页面导入交互。
- 验收：支持正则 `^(\w+)\s*/([^/]+)/\s*(.*)$`，可反馈“成功导入 X 个”。

## 子代理 5：页面交互代理（UI/UX）
- 负责：5 个底部导航页面与详情页交互。
- 产出：`lib/pages/*.dart`。
- 验收：
  - 单词本列表和详情完整
  - 选择题即时反馈并自动下一题
  - 拼写输入与容错提示
  - 错题本可触发重练
  - 统计页展示 4 项指标

## 子代理 6：质量验收代理（QA）
- 负责：编译验证、功能回归、边界条件检查。
- 产出：验收报告（构建、运行、功能点、已知限制）。
- 验收：所有核心功能路径可执行，无阻断错误。

## 协作节奏建议
1. Architect 先建框架并锁定接口。
2. Data 与 Training 并行开发。
3. PDF Import 与 UI 并行开发。
4. QA 最后统一联调回归。

## 当前状态（已落地）
- 已生成完整 Flutter 代码骨架与核心模块。
- 已实现：数据库、5 大功能页、PDF 导入、错题与统计联动。
- 待你本机执行：`flutter pub get` -> `flutter run` 进行真机/模拟器验证。
